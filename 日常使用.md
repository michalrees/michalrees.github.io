# 日常使用

然也的 Blog 日常操作手册。

| 项 | 值 |
|---|---|
| 线上地址 | <https://michalrees.github.io/> |
| 本地仓库 | `E:\Michal\Repos\Blog` |
| 远端 | `https://github.com/michalrees/michalrees.github.io.git` |
| 分支 | `main`（唯一分支，GitHub Actions 云端构建） |
| gh CLI | `C:\Program Files\GitHub CLI\gh.exe`（不在 PATH，用完整路径） |

> 2026-09 仓库已从 `E:\Haoran\Blog` 迁到 `E:\Michal\Repos\Blog`。同步脚本用 `$PSScriptRoot` 定位，换路径不受影响。

## 一句话记住

**原稿只写在 `doc\` → 跑 `.\doc\publish-doc.ps1` 同步到 `source\_posts\` → `git push` → 约 1 分钟上线。**

本地**不需要**装 Hexo，也不需要 `hexo generate` / `hexo deploy`。

## 目录分工

| 位置 | 作用 | 能不能手改 |
|---|---|---|
| `doc\` | **文章原稿，唯一事实源** | ✅ 只改这里 |
| `doc\_模板.md` | 新文章模板 | 复制它起稿 |
| `doc\publish-doc.ps1` | 同步脚本 | 一般不碰 |
| `doc\README.md` | doc 文件夹自己的详细说明 | 可以 |
| `source\_posts\` | Hexo 发布目录（脚本生成的副本） | ❌ **不要手改**，会被覆盖 |
| `source\img\` | 图片资源 | 加图放这里 |
| `_config.yml` | 站点配置（标题、URL、permalink、部署） | 改完必须验证 |
| `_config.butterfly.yml` | 主题配置（菜单、样式、功能开关） | 改完必须验证 |
| `themes\butterfly\` | 主题源码 | 尽量别动，升级会被覆盖 |
| `public\`、`db.json` | 本地预览产物 | 已在 `.gitignore`，不入库 |

## 写新文章

```powershell
cd E:\Michal\Repos\Blog
git pull --ff-only                              # 先同步远端，避免分叉
Copy-Item "doc\_模板.md" "doc\我的新文章.md"     # 从模板起稿
```

然后编辑 `doc\我的新文章.md`。front-matter 至少这三项：

```yaml
---
title: 我的新文章
date: 2026-09-15 10:00:00
description: 一句话摘要（首页卡片、站内搜索、SEO 都用它）
---
```

文件名就是 URL（`permalink: :year/:month/:day/:title/`），**发布后改文件名等于改链接**。
名字里可以带日期前缀（`2026-09-15-标题.md`）方便自己排序，同步时会自动去掉。
完整字段说明见 `doc\README.md`。

## 发布三步

```powershell
cd E:\Michal\Repos\Blog

# 1. 原稿 → 发布目录
.\doc\publish-doc.ps1

# 2. 提交推送
git add .
git commit -m "新文章：我的新文章"
git push

# 3. 等约 1 分钟，GitHub Actions 自动构建上线
```

脚本参数：

| 参数 | 作用 |
|---|---|
| 无 | 同步 doc 下全部文章（幂等，没变的不动） |
| `-Name 我的新文章` | 只同步一篇 |
| `-DryRun` | 只看会发生什么，不写文件 |
| `-Prune` | 顺带列出 `source\_posts\` 里 doc 已无对应原稿的文件，确认后删除 |
| `-Force` | 配合 `-Prune` 跳过确认（慎用） |

> 规则：**只改 `doc\` 里的原稿**，`source\_posts\` 里的文件由脚本单向覆盖。
> 手改 `source\_posts\` 会在下次同步时被覆盖掉。

## 本地预览

```powershell
cd E:\Michal\Repos\Blog
pnpm exec hexo clean
pnpm exec hexo server        # http://localhost:4000
```

预览完记得清理，否则 `public\` 会挡住 `git pull`：

```powershell
pnpm exec hexo clean
Remove-Item public -Recurse -Force -ErrorAction SilentlyContinue
```

## 发布后确认

```powershell
# 最近一次 Actions 运行状态（约 1 分钟后查；queued/in_progress 是正常的）
$gh = "C:\Program Files\GitHub CLI\gh.exe"
$o = ((& $gh api "repos/michalrees/michalrees.github.io/actions/runs?per_page=1" 2>$null) -join '') | ConvertFrom-Json
$r = $o.workflow_runs[0]
"$($r.status) | $($r.conclusion) | $($r.head_sha.Substring(0,7)) | $($r.display_title)"

# 站点是否 200
(Invoke-WebRequest "https://michalrees.github.io/" -UseBasicParsing -TimeoutSec 60).StatusCode
```

`completed | success` 即成功。失败时看日志：

```powershell
& "C:\Program Files\GitHub CLI\gh.exe" run list --repo michalrees/michalrees.github.io --limit 5
& "C:\Program Files\GitHub CLI\gh.exe" run view <run-id> --repo michalrees/michalrees.github.io --log-failed
```

注意：`Invoke-WebRequest` 超时后变量是空的，任何 `-match` 都会误判失败，超时值给大一点。

## 改已有文章 / 删文章

改：编辑 `doc\xxx.md` → `.\doc\publish-doc.ps1` → `git add .; git commit -m "更新：xxx"; git push`

删：删掉 `doc\xxx.md` → `.\doc\publish-doc.ps1 -Prune` → 按提示确认 → 提交推送

## 踩坑速查

**① `git pull` 报 "untracked working tree files would be overwritten"**
本地跑过预览产生 `public\`。解决：

```powershell
pnpm exec hexo clean
Remove-Item public -Recurse -Force -ErrorAction SilentlyContinue
git pull --ff-only
```

**② push 要认证 / 证书错误**
正常情况不需要密码。依次查（这三项应存在）：

```powershell
& "C:\Program Files\GitHub CLI\gh.exe" auth status
git config --global --get-all "credential.https://github.com.helper"
git config --global --get http.sslBackend
git config --global --get "http.https://github.com/.sslCAInfo"
```

**不要试图用 SSH 推这个仓库**，22 端口被封且公钥授权被拒，走 HTTPS + gh token 这条链路已验证可用。

**③ 改了配置但线上没变化**
`hexo generate` 是增量的，先清再建，并**用产物和线上双重验证**：

```powershell
pnpm exec hexo clean; pnpm exec hexo generate
Select-String -Path "public\index.html" -Pattern "要验证的文案"
```

## 写作约定

| 事项 | 现状 |
|---|---|
| 目录 TOC | 已开启，`##` / `###` 自动生成右侧目录 |
| 版权模块 | 已开启，自动附 CC BY-NC-SA 4.0，无需手写 |
| 评论 | **未启用** |
| 数学公式 | **未渲染**。要公式需先把 `_config.butterfly.yml` 的 `math.use` 设为 `katex` |
| Mermaid 流程图 | **未开启**，写了也只当普通代码显示 |
| 站内搜索 | `local_search`，标题/正文/`description` 都可被搜到 |
| 图片 | 放 `source\img\`，正文用 `/img/xxx.png`（开头带斜杠） |
| 置顶 | front-matter 加 `sticky: 1`，数字越大越靠前 |

## 不要做的事

- 不要手改 `source\_posts\`（会被 `publish-doc.ps1` 覆盖）
- 不要在 `_config.yml` 的 `deploy.message` 里用 `now()` / `date()`（中文 locale 下渲染成怪字符串）
- 不要删 `pnpm-workspace.yaml` 里的 `allowBuilds` / `onlyBuiltDependencies`
- 不要把 GitHub Pages 发布源设成 `main / (root)`（Jekyll 会读 Hexo 配置然后报错）
- 不要在 `_config.butterfly.yml` 的 `inject.head` 条目里写不带引号的 `#`（YAML 会当注释，整段静默丢失）
- 不要用 SSH 推仓库；不要手工编辑 `gh-pages` 分支或 Pages 设置
- `E:\Michal\Repos\Blog` 与 `C:\Users\yuhao\michalrees.github.io` 是同一远端的两份克隆，**统一用 E 盘这份**，避免分叉
